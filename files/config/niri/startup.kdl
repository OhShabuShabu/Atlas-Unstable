# Desktop services (awww, vicinae, xwayland-satellite, polkit) are auto-started
# as systemd user services via graphical-session.target.
# Only launch what systemd doesn't handle.

spawn-at-startup "noctalia-shell"
